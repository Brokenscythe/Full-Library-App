<!DOCTYPE html>
<html>
  <head>
    <title>Forgot Password</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;1,400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/password-reset.css">
    <link rel="shortcut icon" href="img/library-favicon.ico" type="image/vnd.microsoft.icon" />
  </head>
  <body>
    <h1>Zaboravio Lozinku?</h1>
    <p>Unesi svoj e-mail, potom ćeš dobiti smjernice.</p>
    
    <form action="/forgot-password" method="POST">
      <% if(inputData.error_message) { %>
        <section class="alert">
          <h3>Pogrešan unos podataka.</h2>
          <p><%= inputData.error_message %></p> 
        </section>
      <% } else { %>
        <% if (inputData.successful_message) { %>
          <section class="success">
            <h3>Email-Confirmation</h3>
            <p><%= inputData.successful_message %></p>
          </section>
        <% } %>
      <% } %>        
      <input type="hidden" name="_csrf" value="<%= locals.csrfToken %>" />
      <label for="email">Email:</label>
      <input type="email" id="email" value="<%= inputData.email %>" name="email" required placeholder="Email"/>
      <button type="submit">Potvrdi</button>
      <a href="/login">Uloguj se</a>
    </form>
  </body>
</html>
